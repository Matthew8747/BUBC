{
  "name": "bubc-site",
  "private": true,
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter @bubc/web dev",
    "build": "pnpm --filter @bubc/web build",
    "preview": "pnpm --filter @bubc/web preview"
  }
}
